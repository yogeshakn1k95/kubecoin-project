apiVersion: v1
kind: ConfigMap
metadata:
  name: kubecoin-config
  labels:
    app: kubecoin
data:
  # Database configuration (non-sensitive)
  POSTGRES_DB: "kubecoin"
  DB_HOST: "postgres-headless"
  DB_NAME: "kubecoin"
  
  # Frontend configuration
  BACKEND_URL: "http://backend-service.default.svc.cluster.local:5000"
